# Copy this file to .env and adjust
PORT=5005
# Keep this secret and stable if you want multi-day correlation
PEPPER="super-long-random-string-change-me"
# If true, API responses from /scan will include raw BSSID (DB still stores hash)
STORE_RAW_BSSID=false
# SQLite file path (relative or absolute). Default is data/ice.db
DB_PATH=data/ice.db
# App version string (for evidence/exports)
APP_VERSION=0.2.0
